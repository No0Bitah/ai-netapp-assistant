export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: 'sans-serif' }}>
      <h1>AI NetApp Assistant — Frontend</h1>
      <p>Vite + React running. Pointing to API: {import.meta.env.VITE_API_BASE}</p>
    </div>
  )
}